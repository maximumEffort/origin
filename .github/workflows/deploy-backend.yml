# ╔══════════════════════════════════════════════════════════════════════════╗
# ║  DECOMMISSIONED — Railway is no longer used.                            ║
# ║                                                                         ║
# ║  Backend deploys now go through Azure Container Apps:                   ║
# ║    .github/workflows/deploy-azure-backend.yml                          ║
# ║                                                                         ║
# ║  See ADR-0001 Phase 5 (Decommission Railway).                          ║
# ║  This file is kept as a no-op stub so GitHub doesn't show a dangling   ║
# ║  workflow reference. It can be deleted entirely once confirmed.         ║
# ╚══════════════════════════════════════════════════════════════════════════╝

name: Deploy backend (Railway) [DECOMMISSIONED]

on:
  workflow_dispatch: {}   # Manual only — no auto-triggers.

jobs:
  decommissioned:
    name: Railway is decommissioned
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: |
          echo "⚠️  Railway deployment has been decommissioned."
          echo "    Backend deploys now use Azure Container Apps."
          echo "    See: .github/workflows/deploy-azure-backend.yml"
          echo "    See: docs/adr/0001-azure-uae-north-architecture.md"
          exit 0
