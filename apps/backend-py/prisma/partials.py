"""
Prisma Partial Types (advanced).

For now, no custom partial types. This file exists so prisma-client-py
doesn't fail when partial_type_generator is set in schema.prisma.

Add partial types here as needed:
    from prisma.partials import CustomerOnly, BookingDetail
"""
# Intentionally empty for V1.
