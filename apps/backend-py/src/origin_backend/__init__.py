"""Origin backend (Python) — FastAPI service.

Mirrors the NestJS backend in apps/backend/. Both run side-by-side during
the migration window; the frontends point at one or the other via
NEXT_PUBLIC_API_URL.
"""

__version__ = "0.1.0"
